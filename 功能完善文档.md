# wapp 功能完善文档

> 本文档用于记录 wapp（小说助手）项目在日常迭代、上线前后过程中的**补充、修复、完善、验证**事项。
> 架构设计、业务规则、数据库结构、界面与主题等"是什么 / 为什么"已经在 `README.md` 里写得很完整，
> 本文档不重复那些内容，只记录**会随时间变化的状态**：发现了什么问题、做了什么修复、正在验证什么、
> 上线前还差什么。

## 使用约定

1. 新条目统一追加到本文档「变更记录」表格的**最下面**，不要覆盖或删除历史记录。
2. 如果某条记录后来被撤销/回滚，不要直接删除该行，改为在「状态」列标注"已撤销"并在「备注」里写明原因和时间，
   保留痕迹（参照本机其它项目的既有惯例：撤销不删记录）。
3. 每条记录建议包含：日期、类型（补充 / 修复 / 完善 / 验证 / 其他）、内容、状态（待处理 / 进行中 /
   已完成 / 已验证 / 已撤销）、备注。
4. 涉及数据库结构变更时，遵照 `README.md`「新增一次迁移」一节的**追加式迁移**约定——
   在 `src/main/db/migrations/index.ts` 数组末尾追加新迁移，不修改已发布的迁移条目。
5. 涉及"拦截 / 校验"类的修复或新增用例，优先补进 `src/main/smoke-test.ts`（走真实 IPC 边界），
   而不是只在服务层测——服务层不重复做校验，直接调服务层测不出校验漏洞。
6. 断言新增 UI 元素时用 `data-testid`，不要依赖 antd 内部类名；涉及几何/可见性问题时优先量
   `getBoundingClientRect()`，不要只做存在性判断（这两条是本项目冒烟测试踩过坑后定下的约定，
   详见 README「五条踩过坑的断言约定」）。
7. 新增的纯函数模块优先补 vitest 单测（`npm run test`）；若逻辑依赖 Electron 运行时（dialog/BrowserWindow 等），
   用 `vi.mock('electron')` 把这些 API 探头掉来单测，不要因为“需要 Electron”就放弃单测、只靠冒烟测试。

## 项目现状速览（记录时间：2026-09-18）

- **技术栈**：Electron + React 19 + TypeScript + SQLite（better-sqlite3）+ Ant Design 6 + TipTap（ProseMirror）
- **定位**：跑在本机的长篇小说写作管理器——管书籍/分卷、写章节正文、看写作节奏，数据全部落在本地
  `%APPDATA%\wapp\wapp.db`，不上传到任何服务器
- **功能模块**（导航共 5 项，`nav.tsx` 中全部 `ready: true`）：
  首页看板 / 书籍管理（书籍·分卷·章节） / 大纲管理（自由多层情节树） / 卡片库（人物·物品·灵感） /
  时间与字数统计
- **三层架构**：IPC handler（控制器，只解析+校验） → Service（业务规则与事务） → Repository（唯一写 SQL 处）
- **跨进程契约**：`src/shared` 是主进程与渲染进程共用的唯一契约来源（类型 + Zod schema），
  改一处两侧同步生效
- **数据库 schema 版本**：`004_chapter_target_words`（G1–G4 本轮没有新增迁移）
- **IPC 通道数**：44 个，注册于 `src/main/ipc/registry.ts`（比 09-18 初次梳理时的 41 个多了 3 个：
  `exporter:book`/`exporter:volume`/`backup:database`）
- **preload 桥接版本**：`BRIDGE_VERSION = '7'`
- **安全边界**：`contextIsolation: true` / `nodeIntegration: false` / `sandbox: true`，前端只能
  拿到 `preload` 白名单里的方法
- **冒烟测试**：`npm run smoke`，当前 **78 项全部通过**（数据接线层 → 业务规则层 → 渲染进程链路层
  三层），报告见根目录 `smoke-report.txt`
- **单元测试**：`npm run test`（vitest），当前 **109 项全部通过**，覆盖 4 个纯函数模块（G1）+
  导出模块的 schema/service（G3，包含 `vi.mock('electron')` 的服务层测试）+ `ChapterRepository.listWithContent`（G3，
  用真实 better-sqlite3 内存数据库）+ 备份服务（G4）

> 本项目的具体实现工作目前由另一 AI 框架/会话负责，本文档只作梳理与状态记录，
> 不对当前正在进行中的实现细节或工作区未提交内容做评判或介入。

## 首次梳理发现的待完善事项（2026-09-18 记录，处理进度见下方）

| 编号 | 类型 | 内容 | 优先级 | 状态 |
|---|---|---|---|---|
| G1 | 完善 | 项目只有 `npm run smoke` 一套端到端脚本，没有常规单元测试框架（vitest/jest 等）。纯函数模块（`src/shared/proofread.ts` 纠错引擎、`doc-text.ts` 文本投影等）目前只能靠 `smoke-test.ts` 间接覆盖，缺少针对边界输入的细粒度单测 | 中 | 已处理（2026-09-18，具体见下方变更记录） |
| G2 | 完善 | 没有 CI/CD 配置（未见 `.github/workflows` 等目录），`typecheck` / `build` / `smoke` 均需手动本地执行，无法在提交/合并时自动把关 | 中 | 按用户要求不处理 |
| G3 | 完善 | 导出功能（`src/main/modules/exporter`）之前只能**单章**导出为 `txt` / `md`，没有整本书/整卷的批量导出 | 低 | 已处理（2026-09-18，具体见下方变更记录；仍不支持 `docx`/`epub` 等格式，未列入本轮范围） |
| G4 | 完善 | 没有内置的数据库备份/恢复能力，只能靠用户手动复制 `%APPDATA%\wapp\wapp.db` 文件来备份；卸载时虽保留该文件（`deleteAppDataOnUninstall: false`），但没有"一键备份"的界面入口 | 低 | 已处理（2026-09-18，仅备份，不含恢复，具体见下方变更记录及原因） |

## 变更记录

| 日期 | 类型 | 内容 | 状态 | 备注 |
|---|---|---|---|---|
| 2026-09-18 | 完善 | 建立本文档，梳理项目现状与首批待完善事项（G1–G4） | 已完成 | 详见上方「首次梳理发现的待完善事项」，具体处理留待后续条目 |
| 2026-09-18 | 完善 | G1：引入 vitest 单测框架（`vitest.config.ts`，与 `electron.vite.config.ts` 同样的 `@shared`/`@renderer` 别名，`npm run test` / `test:watch` 脚本），为四个纯函数模块补上细粒度单测：`src/shared/text.test.ts`（27）、`src/shared/datetime.test.ts`（17）、`src/shared/proofread.test.ts`（27）、`src/renderer/src/features/chapters/doc-text.test.ts`（20，用 `@tiptap/pm/model` 的 `Schema` 手建真实 ProseMirror 文档并以 `doc.textBetween` 作为交叉验证 oracle）。过程中发现并修正了两处我对 `htmlToText`/`proofread` 行为的预期误判（段间分隔符会重复插入产生双换行；半角省略号的首个点会同时命中 mixedScript 规则），未发现真实产品代码缺陷。 | 已完成 | 82 项单测全部通过，`npm run typecheck` 与 `npm run smoke`（78/78）均未回归 |
| 2026-09-18 | 完善 | G3：导出功能新增整本书/整卷批量导出。共享层新增 `exportBookSchema`/`exportVolumeSchema`/`ExportBatchResult`（`src/shared/modules/exporter.ts`）与两个 IPC 通道 `exporter:book`/`exporter:volume`；`ChapterRepository` 新增 `listWithContent()`（与 `list()` 同一套排序规则，只多选一列 `content_text`，保证列表页与导出文件里的章节顺序永远一致）；`ExportService` 新增 `exportBook`/`exportVolume`（无章节可导时报 `VALIDATION_ERROR`，而不是导出一个空文件），与 `exportChapter` 共用新抽出的 `saveDraft` 弹框/写盘逐段；preload `BRIDGE_VERSION` 从 5 升到 6（先升到6、再根据 G4 升到7，见下一条）；前端 `BookDetailPage.tsx` 新增顶部「导出整本书」按钮与每卷行内的「导出本卷」按钮。单测用 `vi.mock('electron')` 探头 dialog/BrowserWindow 来无需真实桥接就能测服务层，`ChapterRepository` 的测试用真实 better-sqlite3 内存数据库 + 项目自己的迁移脚本建表。 | 已完成 | 新增 24 项单测（schema 11 + service 8 + repository 5）全部通过；`npm run typecheck` 、`npm run smoke`（78/78，通道数 43→后面 G4 又加一个变 44）均未回归 |
| 2026-09-18 | 完善 | G4：新增数据库备份能力（仅备份，不含恢复——恢复需要安全地关闭/重开一个被各 Repository/HealthService 共享引用的活连接，复杂度超出一次性小功能范围，此决定已写入 `src/shared/modules/backup.ts` 的注释里，恢复仍靠手动：关闭 wapp 后用备份文件覆盖 `%APPDATA%\wapp\wapp.db`）。新增 `BackupService.backupDatabase()`，用 better-sqlite3 自带的 `db.backup()` 而不是直接 fs 复制 .db 文件，因为连接开的是 WAL 模式，直接复制主文件可能漏掉还没 checkpoint 的最新写入。新增 IPC 通道 `backup:database`；前端在顶栏（`AppHeader.tsx`，与 HealthBadge/ThemeToggle 同行，因为这是全局操作而不是某一本书的操作）新增「备份数据库」图标按钮；preload `BRIDGE_VERSION` 升到 7。 | 已完成 | 新增 3 项单测（`vi.mock('electron')` + `vi.mock('../../db/connection')` 探头）全部通过；全项目单测共 109 项、`npm run typecheck`、`npm run smoke`（78/78，通道数 44）均未回归 |

## 附：文档定位说明

- **`README.md`**：架构设计、模块业务规则、数据库表结构、IPC 契约、界面与主题决策的**权威技术参考**。
  改代码、理解"为什么这么设计"时以它为准，不要在本文档里重复写这些内容。
- **本文档（`功能完善文档.md`）**：只记录"状态变化"——发现的问题、做过的修复、正在验证的事项、
  上线前后的补充清单。技术细节变了就去改 README，状态变了就在这里追加一行。
